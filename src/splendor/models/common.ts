export enum GemType {
    Gold = 0,
    Black = 1,
    White = 2,
    Red = 3,
    Blue = 4,
    Green = 5,
}

export enum CardCategory {
    Low = 0,
    Medium = 1,
    High = 2,
}

export enum ActionType {
    TakeGems = 0,
    ReserveCard = 1,
    PurchaseCard = 2
}
